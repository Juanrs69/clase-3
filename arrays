 <!--¿Qué son los arrays en JavaScript?*
   /*Los arrays en JavaScript son objetos que permiten almacenar múltiples valores en una sola variable.
    Estos valores pueden ser de cualquier tipo, incluyendo otros arrays, lo que permite crear estructuras de datos complejas.
    Los arrays en JavaScript son dinámicos, lo que significa que su  longitud puede cambiar durante la ejecución del programa.*/-->
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Arrays en JavaScript</title>
    </head>
    <body>
        <h1>Arrays en JavaScript</h1>
        <script>
            // A) Crear un arreglo con 4 elementos y mostrar su último elemento
            let arreglo1 = ['elemento1', 'elemento2', 'elemento3', 'elemento4'];
            console.log("El último elemento del primer arreglo es:", arreglo1[arreglo1.length - 1]);
    
            // B) Crear un arreglo con todos los números pares menores que 10
            let numerosPares = [];
            for (let i = 2; i < 10; i += 2) {
                numerosPares.push(i);
            }
            console.log("Array de números pares menores que 10:", numerosPares);
            console.log("Cantidad de elementos en el array:", numerosPares.length);
    
            // Agregar 1, 3, 5, 7, 9 al final del array y mostrarlo nuevamente
            numerosPares.push(1, 3, 5, 7, 9);
            console.log("Array con números pares y los números 1, 3, 5, 7, 9:", numerosPares);
    
            // Insertar el valor 20 en el vigésimo elemento del array y mostrarlo nuevamente
            numerosPares[19] = 20;
            console.log("Array con el valor 20 en el vigésimo elemento:", numerosPares);
        </script>
    </body>
    </html>
    
